node_modules
dist
.DS_Store
*.local
*.tsbuildinfo
# Сгенерированные tsc-компаньоны vite.config.ts (tsconfig.node.json их
# эмитит рядом при tsc -b) — не исходники, пересоздаются сами.
vite.config.js
vite.config.d.ts
# ^ это правило уже покрывает .env.local — ключи Supabase не попадут в git.
# Пример того, что туда писать — в .env.local.example (он в репозитории).

# Исходные данные ВКР с реальными ФИО/датами рождения пациентов (не для
# публичного репозитория/деплоя — приложение использует только анонимизированные коды).
/Diploma
/private

# Тот же случай, но уже внутри src: реальные ФИО/ДР 9 пациентов группы
# Миопия для страницы /myopia. Страница есть в коде и работает локально,
# но сам файл с данными — не в репозитории, пока не решено иначе.
/src/data/myopiaIdentity.ts

# Рабочие материалы (референсы для Figma, скриншоты, PDF из ВКР) — лежат
# прямо в папке проекта, но это не исходники приложения и не для публичного
# репозитория/деплоя.
/Референсы
/*.pdf
